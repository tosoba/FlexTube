<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="homeView"
            type="com.example.there.flextube.home.HomeView" />
    </data>

    <com.otaliastudios.nestedscrollcoordinatorlayout.NestedScrollCoordinatorLayout
        android:id="@+id/home_coordinator_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".home.HomeFragment">

        <android.support.design.widget.AppBarLayout
            android:id="@+id/home_app_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:orientation="vertical">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/video_categories_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adapter="@{homeView.videoCategoriesAdapter}"
                android:visibility="@{(safeUnbox(homeView.state.videoCategories.size()) > 0) ? View.VISIBLE : View.GONE}"
                app:layout_scrollFlags="scroll|enterAlways" />

        </android.support.design.widget.AppBarLayout>

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{(safeUnbox(homeView.state.homeItems.size()) > 0) ? View.GONE : View.VISIBLE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/home_items_recycler_view"
            itemDecoration="@{homeView.itemDecoration}"
            onScrollListener="@{homeView.onScrollListener}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adapter="@{homeView.videosAdapter}"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    </com.otaliastudios.nestedscrollcoordinatorlayout.NestedScrollCoordinatorLayout>

</layout>

